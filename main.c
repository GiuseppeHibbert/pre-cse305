#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX_VAL 999
int main(int argc, char * argv[]){

    // load the csv file in for input.

    
    printf("Hello world.\n");

    return 0;
}

void csv_reader(const char *filename){
    File *csv_file = fopen(filename, "r");
    char buffer[MAX_VAL]
    while (fgets(line,MAX_VAL,csv_file)) {    

    }
    fclose(csv_file); 
}

 
